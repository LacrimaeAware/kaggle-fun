# 2026-06-17

Big ingestion + tooling day. The user downloaded the official data tab and a pile of
notebooks/discussions; processed all of it and built the viewer.

## Done
- Opened the official competition zip. Major corrections (see docs/COMPETITION.md "Update
  2026-06-17"):
  - The forward-model SEARCH API exists (`cg/api.py` `search_begin`/`search_step`). H1 was
    wrong (refuted on the stripped installed package); corrected to supported. In-match search
    is the intended design.
  - Full card data: `EN_Card_Data.csv`, 1267 cards. Built `tools/cards.py`.
  - Option-type legend confirmed (7 PLAY, 8 ATTACH, 13 ATTACK, 14 END, ...).
  - Deck-building confirmed open (Kaggle staff). Prize rules: ex KO = 2 prizes, megaEx = 3.
  - Our deck decoded = Mega Abomasnow ex, the worst sample deck.
- Scraped all 1267 card images from the PDF (`tools/scrape_card_images.py`), 134MB, gitignored.
- Deck store: `registry/decks.json` (5 decks; `tools/build_decks.py`). Auto-names imperfect
  (the Crustle deck got tagged "Koraidon ex"); renameable.
- Replay parser + stats store (`tools/parse_replay.py`, `tools/build_stats.py`). Only the
  self-validation replay so far, so stats are thin.
- HTML viewer (`viewer.html`, `tools/build_viewer.py`): Cards / Decks / Stats / Replays, sidebar
  nav, dark themed, color-coded by energy. JS syntax-checked; NOT visually verified (the
  computer-use access dialog timed out while the user was away).
- Digested all 21 notebooks into `research/notebooks/SUMMARY.md` (24-agent parallel read). Key
  findings: only 2 notebooks actually use the search API; "lb-860" is a filename not a score;
  reusable eval functions + crash-safe layer + self-play harness identified; opponent modeling
  is the unexploited edge (H22). Deleted 1 misfiled UMUD notebook + 1 dup.
- Wrote the 3-stage plan of attack (docs/PLAN.md), logged the embeddings idea measuredly
  (IDEAS.md), updated COMPETITION.md / LANDSCAPE.md / AGENTS.md.

## Honest notes
- The viewer is built but unseen by anyone. First open may surface visual issues; fix on report.
- Several Abomasnow variants in the deck store are near-duplicates; fine for study.
- Stats/replay views are structurally done but data-starved until real ranked replays arrive.

## Next (Stage 1 in PLAN.md)
- Crash-safe wrapper + board-aware heuristic eval (reuse the digest's eval pieces) + deck swap.
  Gate: beat first_agent in self-play.

## Later 2026-06-17: heuristic battler measured (negative), pivot to functional features + RL

- Hand-scored battler measured: aggressive version LOST to first_agent (0.44), conservative
  ties it (0.500), both beat random (~0.90). So hand-scoring does not beat the baseline.
  agent/main.py left as the safe conservative baseline. Recorded; do not re-try plain
  feature-scoring (re-open only with consequence-evaluation via search).
- User redirected: stop dumb heuristics, get clever, do RL, agree on approach before building.
  Saved as memory [[feedback-agree-before-building]].
- Built the functional-classification foundation (the user's idea): tools/classify_cards.py
  (rule pass) + an LLM human-in-the-loop labeling tool, tools/review_server.py. It runs a
  local web app (http://localhost:8771); a background worker asks LM Studio
  (meta-llama-3.1-8b-instruct) to tag each of 1267 cards with functional TCG roles, and the
  user reviews an uncertain-first, class-balanced queue (done/not-done tabs, editable, add-a-
  class). Store: registry/card_review.json (resumes). LM Studio also has bge-m3 / nomic
  embedding models available for the embedding work later.
- Card data dumped from the engine: data/external/official/cards_full.json (skills/effect
  text), agent/attack_stats.json (attackId->damage, 1556), agent/card_stats.json.
- Plan agreed: functional classification (confirmed labels) -> features for heuristics +
  embeddings + the RL+search agent (adapt the organizers' RL+MCTS sample). RL is the main
  build next, using these features + a real opponent prior. Resume the review tool with
  `python tools/review_server.py`.
