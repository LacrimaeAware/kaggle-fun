# Starmie Conservative Selector V2 Portable Export

Default mode must stay off. Use `mode="c3_family_limited"` only inside the requested second tiny smoke.

Call:
`runtime.rank_and_select(packed, packed['packed_options'], baseline_action=packed.get('baseline_action'), search_action=packed.get('search_action'), mode='c3_family_limited')`

Selected policy: `C3_FAMILY_LIMITED_SELECTOR`.
Allowed override families: ATTACH, SELECT_CARD, EVOLVE, PLAY.
Blocked live override families: ATTACK, END, RETREAT.

This bundle is disabled/offline. Do not submit, merge, tune thresholds, or enable by default from this export.

Parity passed: True
Packer parity passed: True
