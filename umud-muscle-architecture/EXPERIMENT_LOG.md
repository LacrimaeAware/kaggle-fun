# UMUD Experiment Log

One place to see what we tried, what it scored, the exact program/flag that produced it, and how to
roll back. Newest at top. Append, do not rewrite history.

Conventions: "LB" = Kaggle public leaderboard (lower is better). "bench" = 35-expert OSF benchmark
(local, CPU, NOT an oracle - see the leakage/recenter notes below). Safe baseline file:
`Downloads/0P61918_submission_local.csv` (LB 0.61918). `results/` is gitignored.

Long-term feature tracking now lives in `FEATURE_DATABASE.md` and `FEATURE_DATABASE.csv`. New ideas
should be added there even before implementation, and every benchmark/public delta should name the
baseline it was deltaed from.

## Submissions (what actually hit the leaderboard)

| date | method | LB | changed | how to reproduce / roll back | status |
|------|--------|----|---------| -----------------------------|--------|
| 2026-06-13 | local-benchmark proxy plus missing-scale patch (`submission_burn_28_local_benchmark_proxy_plus_missing_scale.csv`) | 0.65917 | burn #16 FL proxy + burn #17 MT proxy + 4-row missing-scale patch; PA unchanged | `results/submission_burn_28_local_benchmark_proxy_plus_missing_scale.csv`; see `EXP71_LOCAL_BENCHMARK_PROXY_SCALE_STACK_2026-06-13.md` | rejected hard; not just burn #15 plus scale, and not evidence for promoting the proxy stack |
| 2026-06-13 | guarded field-depth broad scale probe (`submission_burn_22_field_depth_guarded_scale_probe.csv`) | 0.66197 | broad field-depth scale override on 114 rows | `results/submission_burn_22_field_depth_guarded_scale_probe.csv`; see `EXP67_FIELD_DEPTH_SCALE_PROBE_2026-06-13.md` | rejected hard; broad field-height scale overrides are dangerous |
| 2026-06-13 | current best + robust triangle + vertical-MT proxy (`submission_burn_17_core_plus_vertical_mt_proxy.csv`) | 0.60720 | burn #15 plus vertical-MT proxy delta | `results/submission_burn_17_core_plus_vertical_mt_proxy.csv`; see `EXP57_SUBMISSION_SPLIT_STACK_2026-06-13.md` | rejected; MT proxy worsened vs 0.58910 and vs burn #15 |
| 2026-06-13 | current best + robust triangle + visibility-weighted FL proxy (`submission_burn_16_core_plus_visibility_weighted_fl_proxy.csv`) | 0.64511 | burn #15 plus broad visibility-weighted FL proxy delta | `results/submission_burn_16_core_plus_visibility_weighted_fl_proxy.csv`; see `EXP57_SUBMISSION_SPLIT_STACK_2026-06-13.md` | rejected; broad FL support proxy did not transfer |
| 2026-06-13 | current best + robust triangle (`submission_burn_15_temporal_subpixel_shape_ocr_robust_triangle.csv`) | 0.60102 | burn #13 plus robust-triangle geometry delta | `results/submission_burn_15_temporal_subpixel_shape_ocr_robust_triangle.csv`; see `EXP57_SUBMISSION_SPLIT_STACK_2026-06-13.md` | rejected for public baseline; benchmark win did not transfer enough |
| 2026-06-12 | current best + top-3 minimal-extrapolation FL (`submission_burn_14_temporal_subpixel_shape_fl_min_extrap_top3.csv`) | 0.62994 | burn #11 plus broad FL combiner change on 307 rows | `results/submission_burn_14_temporal_subpixel_shape_fl_min_extrap_top3.csv` | rejected; broad FL combiner harmed transfer |
| 2026-06-12 | current best + isolated IMG_00275 OCR scale (`submission_burn_13_temporal_subpixel_shape_img00275_ocr_scale.csv`) | **0.58910** | burn #11 plus one-row OCR scale fix | `results/submission_burn_13_temporal_subpixel_shape_img00275_ocr_scale.csv` | public-score neutral vs #11 |
| 2026-06-12 | temporal + subpixel + clean shape-neighbor fallback scale (`submission_burn_11_temporal_subpixel_shape_neighbor_scale.csv`) | **0.58910** | burn #6 plus 10 stable fallback-row scale corrections | `results/submission_burn_11_temporal_subpixel_shape_neighbor_scale.csv`; follow-ups in `SUBMISSION_BURN_AFTER_SHAPE_WIN_2026-06-12.md` | **NEW BEST** |
| 2026-06-12 | temporal smoothing + subpixel scale precision (`submission_burn_06_temporal_subpixel_scale.csv`) | **0.60936** | temporal file plus tiny gated scale precision deltas | `results/submission_burn_06_temporal_subpixel_scale.csv`; follow-ups in `SUBMISSION_BURN_AFTER_SUBPIXEL_WIN_2026-06-12.md` | superseded by shape-neighbor stack |
| 2026-06-12 | temporal smoothing at clip threshold 0.92 (`submission_burn_04_temporal_smooth_092.csv`) | **0.60961** | PA/FL/MT in 28 sequence-like clips | `results/submission_burn_04_temporal_smooth_092.csv`; follow-ups in `SUBMISSION_BURN_AFTER_TEMPORAL_WIN_2026-06-12.md` | superseded by temporal+subpixel |
| 2026-06-09 | scale router + inner-edge MT + fragment-extrapolation FL | **0.61918** | full pipeline | `Downloads/0P61918_submission_local.csv`; flags: FL_FACING=0, identity_blend=0, MT=perp_center, subpixel=0 | previous best / protected baseline |
| 2026-06-10 | facing-geometry FL (`UMUD_FL_FACING=1`) | 0.66459 | FL only | now default-off; use `UMUD_FL_FACING=1` only to reproduce/test repairs | rejected (bend real, gate wrong 41% - see diagnosis) |
| 2026-06-?? | FL identity blend | 0.63905 | FL only | `FL_IDENTITY_BLEND=0` to undo | rejected |
| 2026-06-?? | MT vertical-3 | 0.62561 | MT only | reverted | rejected |
| 2026-06-?? | bar-only scale tail | 0.66711 | 4 scale rows + FL recenter ripple | reverted | rejected |

## Verified diagnostics (facts, with how each was checked)

| finding | verified how | implication |
|---------|--------------|-------------|
| **FL recenter is a latent global mean device, but a no-op on the shipped 0.619 pipeline.** The code can multiply every FL by `74.424/mean` (segment_then_measure.py:762); on the current baseline the pre-recenter mean is already 74.4, so the multiplier is effectively 1. | read the code; confirmed with `UMUD_FL_RECENTER=0`: 0/309 rows changed | not a hidden confounder for the current baseline; any new FL method that changes the mean must report with/without this step. |
| **The mean and the scale problem are the same root.** | algebra: ratio-anchoring `FL=(FL_px/MT_px)*MT_mm` reduces to `FL_px/scale` | fixing scale deletes the mean. |
| **Bench 0.227 is NOT leakage/overfit-inflated.** 21/35 bench images are in the U-Net training set (im_07 = image_0512 at corr 1.000), but leaked images score WORSE (0.253) than novel (0.189). | content-correlation leak scan + per-image split, score_weights pipeline | 12 epochs did not memorize; bench is honest on that axis. |
| **Bench FL term IS flattered by recentering.** score_weights.py:54 rescales predicted FL to the bench TRUE mean before scoring: FL term 0.534 raw -> 0.353. Raw FL bias is +6.0mm (66.8 vs 60.8). | ran pipeline on 35, computed with/without recenter | bench FL can never reveal a wrong mean; honest bench is ~0.288 not 0.227. |
| **PRIOR is not the bench mean.** PRIOR fl=74.424 vs bench true 60.835 (off by 13.6mm > tolerance). | compared PRIOR to truth means | production ships an FL mean the bench never validated. |
| **Printed depth is OCR-readable.** "De 50 mm" / numbered ruler "...3.5cm" read off the UI with easyocr + a `0-9.cm` allowlist. Reads a depth on 23/24 sampled images; clean on the De-50mm family. | scale_ocr.py smoke test | scale can come from the printed depth, not an assumed tick interval. |

## In progress (not wired / not submitted)

| build | file | state | next |
|-------|------|-------|------|
| **EXP78 scale review and recall segmentation synthesis** | `EXP78_SCALE_REVIEW_AND_RECALL_SEGMENTATION_STATE_2026-06-14.md` | BUILT 2026-06-14. Consolidates the full 309-image manual depth review, the repaired algorithm-only 309/309 displayed-depth audit, the boundary that depth is not trusted `px/cm`, the failed scale probes #22/#28, the downloaded partial EXP72 audit, and the user's recall-heavy segmentation diagnosis. | after EXP77 produces weights, make inference-only recall-heavy variants before spending another full training run; for scale, build a trusted span detector before any broad field-depth correction |
| **EXP77 best-effort heavy segmentation notebook** | `EXP77_BEST_EFFORT_SEGMENTATION_NOTEBOOK_2026-06-14.md`, `kaggle_seg77_best_effort_heavy_auto.ipynb` | BUILT 2026-06-14. Current recommended overnight Kaggle notebook when the goal is strongest implemented segmentation candidate first. Pipeline version `2026-06-14.01` adds CLDice-style topology loss modes. Main run is `seg77_01_best_unetpp640_dilate_soft5_cldice`; alternates test no-topology-loss U-Net++, high-resolution U-Net, and CLAHE U-Net++. Saves run configs, logs, status JSON, summary CSV, submissions, calibration debug CSVs, weights, and debug masks. | run tonight; download `umud_seg77_best_effort_outputs.zip`; inspect `seg77_best_effort_summary.csv` and `pred_debug_*`; first intended candidate is `submission_seg77_01_best_unetpp640_dilate_soft5_cldice.csv` if sane |
| **EXP76 controlled diagnostic segmentation notebook** | `EXP76_TONIGHT_NOTEBOOK_AUDIT_2026-06-14.md`, `kaggle_seg76_controlled_diagnostics_auto.ipynb` | BUILT 2026-06-14. No-edit Kaggle notebook that keeps public-safe measurement settings fixed and tests controlled segmentation axes: binary control, soft target, dilated target, Tversky loss, U-Net++ architecture, and optional soft+Tversky combo. It saves run configs, logs, status JSON, summary CSV, submissions, calibration debug CSVs, weights, and debug masks. | secondary diagnostic after EXP77; download `umud_seg76_controlled_diagnostics_outputs.zip` if run; record summary and any public scores before selecting a new branch |
| **External ultrasound/Kaggle method review** | `EXP75_EXTERNAL_ULTRASOUND_AND_KAGGLE_METHOD_REVIEW_2026-06-14.md` | BUILT 2026-06-14. Broad method review over muscle-ultrasound measurement pipelines, Kaggle segmentation practice, pseudo-labeling, masked ultrasound pretraining, and scale auxiliary modeling. Key correction: the next serious work is not just longer training; it is a classical fascicle-line extractor harness plus EXP74-style diagnostics and controlled GPU ablations. | build `experiments/exp75_classical_fascicle_extractor.py`; update EXP74 notebook generation to include folds/OOF-style diagnostics, threshold/TTA sweeps, probability maps, and geometry summaries |
| **Curve/correction ablation** | `EXP38_CURVE_CORRECTION_ABLATION_2026-06-12.md`, `experiments/exp38_curve_correction_ablation.py` | BUILT 2026-06-12. Reduced expert-benchmark harness over robust triangle, smooth bottom-edge curve, partial curve blends, rotate-only local non-crossing correction, support weighting, and area-style MT. Anchor reproduces robust triangle `0.170`; pure smooth curve overcorrects (`0.273`, FL `0.598`); best local variant is `blend25_curve_rotate` at `0.155` overall, FL `0.238`, MT `0.073`. Bundle for viewer reconstruction is in ignored `results/exp38_curve_correction_ablation/`. | not production/submission-ready; next is wire `blend25_curve_rotate` behind an explicit flag and make its geometry inspectable in the viewer before any slot |
| **PA lower-boundary/local conflict ablation** | `EXP39_PA_LOWER_BOUNDARY_ABLATION_2026-06-12.md`, `experiments/exp39_pa_lower_boundary_ablation.py` | BUILT 2026-06-12. Isolated PA-only harness on top of robust triangle. Lower-boundary tangent PA worsens (`PA 0.150 -> 0.213` smooth, `0.187` quartile polyline). Plain local smoothing worsens (`0.154`/`0.159`). Conflict-gated local correction slightly helps (`PA 0.144`, overall `0.168`) by replacing only clear local angle conflicts. FL/MT intentionally unchanged. Bundle is in ignored `results/exp39_pa_lower_boundary_ablation/`. | small diagnostic/add-on only; do not submit alone. If a larger geometry candidate survives, inspect and wire `pa_conflict_gated_7deg` as an opt-in stack |
| **Untested feature benchmark batch** | `EXP40_UNTESTED_FEATURE_BENCHMARK_2026-06-12.md`, `experiments/exp40_untested_feature_benchmark.py` | BUILT 2026-06-12. Tests feature-database untested rows against the robust-triangle benchmark anchor. Strict scan-region linear support weighting for FL only improves local overall `0.170 -> 0.159` and FL `0.278 -> 0.245`; the harsh visible+scan squared weighting is rejected (`0.198`, FL `0.361`), and support-weighting PA is rejected (`PA 0.205`). Lower-edge quartile median polyline helps FL-only slightly (`0.167`) but worsens MT (`0.179` MT-only). Bundle is in ignored `results/exp40_untested_feature_benchmark/`. | inspect/stack the gentle scan-region FL-only feature; keep lower-edge MT replacement rejected for now; extract per-band benchmark harness next |
| **Per-band benchmark** | `EXP41_PER_BAND_BENCHMARK_2026-06-12.md`, `experiments/exp41_per_band_benchmark.py` | BUILT 2026-06-12. Benchmark-only extraction of the old per-gap/per-band idea. 12/35 expert images have >2 detected bands. Naive per-band fragment-count averaging worsens overall `0.170 -> 0.174` despite improving PA `0.150 -> 0.146`; largest-gap-only is neutral/slightly worse `0.171`. On the multi-band subset, averaging improves PA/MT but worsens FL (`0.205 -> 0.249`). Bundle is in ignored `results/exp41_per_band_benchmark/`. | do not use naive per-band averaging; only revisit as a targeted routing/filtering mechanism |
| **Per-band PA/MT isolation** | `EXP42_PER_BAND_PA_MT_ISOLATION_2026-06-12.md`, `experiments/exp42_per_band_pa_mt_isolation.py` | BUILT 2026-06-12. Freezes FL and swaps only PA/MT from per-band aggregations. Fragment-count average across detected bands gives a small real PA/MT gain: overall `0.170 -> 0.169`, PA `0.150 -> 0.146`, MT `0.083 -> 0.082`. Simple band averaging is rejected (`0.177`). Bundle is in ignored `results/exp42_per_band_pa_mt_isolation/`. | keep as a small PA/MT add-on candidate; not a main lever |
| **PA/MT geometry conventions** | `EXP43_PA_MT_GEOMETRY_CONVENTIONS_2026-06-12.md`, `experiments/exp43_pa_mt_geometry_conventions.py` | BUILT 2026-06-12. Tests PA and MT measurement conventions with FL frozen. PA relative to upper-boundary tangent or average-boundary tangent is strongly rejected (`PA 0.333-0.609` vs `0.150`). MT vertical center gap improves MT `0.083 -> 0.070` and overall `0.170 -> 0.166`; three-position/mean-width MT variants worsen. Bundle is in ignored `results/exp43_pa_mt_geometry_conventions/`. | inspect/wire vertical-center MT as a small geometry candidate; keep lower-boundary PA convention |
| **Best local feature stack** | `EXP44_BEST_LOCAL_FEATURE_STACK_2026-06-12.md`, `experiments/exp44_best_local_feature_stack.py` | BUILT 2026-06-12. Combines isolated local winners: strict scan-region linear FL, PA conflict gate or per-band PA, and vertical-center MT. Best local composition is `0.153` overall (`PA 0.144`, `FL 0.245`, `MT 0.070`) vs robust anchor `0.170`. Bundle is in ignored `results/exp44_best_local_feature_stack/`. | research anchor only; inspect and production-wire components before any public test |
| **PA orientation weird batch** | `EXP45_PA_ORIENTATION_WEIRD_BATCH_2026-06-12.md`, `experiments/exp45_pa_orientation_weird_batch.py` | BUILT 2026-06-12. Tests circular means, visible-length weighting, endpoint-axis orientation, RANSAC, cv2 fitLine, and x/y orientation fields while freezing FL/MT. All meaningful alternatives worsen PA; cv2 fitLine is effectively identical to current PCA. | do not replace current PCA+area-median PA globally; focus on local conflict/family logic |
| **PA raw texture orientation** | `EXP46_PA_RAW_TEXTURE_ORIENTATION_2026-06-12.md`, `experiments/exp46_pa_raw_texture_orientation.py` | BUILT 2026-06-12. Uses grayscale structure-tensor orientation around fragments as a PA estimator/refinement. Raw texture PA is decisively bad (`PA 0.623-0.642`), and blending toward it is also bad. | raw texture may be QA only, not a direct PA estimator |
| **PA contrarian texture** | `EXP47_PA_CONTRARIAN_TEXTURE_2026-06-12.md`, `experiments/exp47_pa_contrarian_texture.py` | BUILT 2026-06-12. Moves PCA orientation away from raw texture. A 10% move fixes signed PA bias (-0.32 -> -0.01 deg) but worsens MAE (`PA 0.150 -> 0.157`); larger moves are rejected. | do not use texture residual as PA correction; signed bias alone is not enough |
| **Geometry class feature matrix** | `EXP48_GEOMETRY_CLASS_FEATURE_MATRIX_2026-06-12.md`, `experiments/exp48_geometry_class_feature_matrix.py` | BUILT 2026-06-12. Scores existing feature candidates by coarse geometry classes and tests story-based stacks. Best story stack is `0.151` overall (`PA 0.140`, `FL 0.242`, `MT 0.070`) by using scan-region FL on low-support rows, per-band PA on multi-band rows, conflict PA elsewhere, and vertical-center MT. Bundle is in ignored `results/exp48_geometry_class_feature_matrix/`. | promising research anchor; inspect in viewer before production wiring; avoid overfitting class gates |
| **Story weight grid** | `EXP50_STORY_WEIGHT_GRID_2026-06-12.md`, `experiments/exp50_story_weight_grid.py` | BUILT 2026-06-12. Tests the weighted-mean diagnosis directly using area, ultrasound-field fraction, visible support, crossing downweight, and local trajectory-residual weights. Best benchmark combo is `0.144` overall (`PA 0.150`, `FL 0.210`, `MT 0.070`) with PA median, raw-span weighted-trimmed FL, and vertical-center MT. Best same-story PA+FL shared reducer is `0.149` overall (`PA 0.158`, `FL 0.220`, `MT 0.070`). Bundle is in ignored `results/exp50_story_weight_grid/`; viewer v2 exposes both models and per-line support weights. | research anchor only; support-weighted FL is locally strong, but PA still needs a real trajectory/curve model and public transfer is unproven |
| **PA gate search** | `EXP51_PA_GATE_SEARCH_2026-06-12.md`, `experiments/exp51_pa_gate_search.py` | BUILT 2026-06-12. Diagnostic-only gate search over PA variants from EXP50. PA median baseline is `0.150`; best gate applies `raw_wtrim10_area_us_visible_frac` only on `boundaries_not_parallel` rows and improves PA to `0.145` (`overall 0.164`). Bundle is in ignored `results/exp51_pa_gate_search/`. | useful PA clue, not a production gate; inspect visually and validate on target labels before trusting |
| **Saturating support / position grid** | `EXP52_SATURATING_SUPPORT_POSITION_GRID_2026-06-12.md`, `experiments/exp52_saturating_support_position_grid.py` | BUILT 2026-06-12. Tests saturating visible-fragment length, US-field support, and position along projected span. Best combo is `0.147` (`PA 0.150`, `FL 0.222`, `MT 0.070`), which is sane but does not beat EXP50. Bundle is in ignored `results/exp52_saturating_support_position_grid/`. | keep the mechanism alive for target-label scoring; not the current local best |
| **Median / weighted reducer blends** | `EXP53_MEDIAN_WEIGHT_BLENDS_2026-06-12.md`, `experiments/exp53_median_weight_blends.py` | BUILT 2026-06-12. Blends median anchors with weighted PA/FL reducers. Best local benchmark score so far is `0.143` (`PA 0.149`, `FL 0.211`, `MT 0.070`): PA median blended 25% toward EXP52 saturating support/position PA, FL median blended 85% toward EXP50 weighted-trimmed FL. Bundle is in ignored `results/exp53_median_weight_blends/`; viewer v2 exposes `Median/weight blend best`. | current local research best; public transfer unproven, so inspect and score on target labels before serious promotion |
| **Model class matrix** | `EXP54_MODEL_CLASS_MATRIX_2026-06-12.md`, `experiments/exp54_model_class_matrix.py` | BUILT 2026-06-12. Scores every expert-viewer model overall and by geometry class. Overall winner is `median_weight_blend_best` at `0.143337`; `story_weight_grid_best` is almost tied (`0.143535`) and wins many curved/sparse classes; `story_stack` has best PA (`0.139936`) but worse FL. Bundle is in ignored `results/exp54_model_class_matrix/`. | use for class-aware routing decisions; validate any overfit class gate against target labels before public submission |
| **Class route search** | `EXP55_CLASS_ROUTE_SEARCH_2026-06-12.md`, `experiments/exp55_class_route_search.py` | BUILT 2026-06-12; corrected to exclude `DLTrack`, `SMA`, and `our_pipeline_true_scale` from the route pool. Starting from `median_weight_blend_best` (`0.143`), model-level greedy routing still improves to `0.135` (`PA 0.147`, `FL 0.188`, `MT 0.072`); term-level routing is `0.131` after removing reference/baseline rows. Bundle is in ignored `results/exp55_class_route_search/`. | class-aware routing has real local headroom; translate only production-owned pieces and validate on target labels before public test |
| **Term route ablation** | `EXP56_TERM_ROUTE_ABLATION_2026-06-13.md`, `experiments/exp56_term_route_ablation.py` | BUILT 2026-06-13. Ablates the corrected allowed-only EXP55 term route. Full route is `0.131264`; prefix-5 / first-five-large is `0.132442`, so most of the route gain is not from tiny late gates. Grouped ablations: FL-only `0.137108`, PA-only `0.139363`, MT-only `0.141467`, PA+FL `0.133133`. Bundle is in ignored `results/exp56_term_route_ablation/`. | use this to split production tests: FL support is the largest route family, PA route is smaller but real, MT route is lowest confidence |
| **Submission split stack** | `EXP57_SUBMISSION_SPLIT_STACK_2026-06-13.md`, `experiments/exp57_submission_split_stack.py` | BUILT 2026-06-13; PUBLIC-TESTED. Creates concrete production-delta CSVs from the public-best anchor because the `0.131264` route is not yet production-wired. Public scores: #15 robust triangle `0.60102`, #16 robust+visibility-FL proxy `0.64511`, #17 robust+vertical-MT proxy `0.60720`. Benchmark equivalents were locally good/mixed, but all public tests regressed vs `0.58910`. | reject these proxy stacks as defaults; next work must production-wire and target-label-check weighted/class routes before more broad geometry burns |
| **Scale status audit** | `EXP58_SCALE_STATUS_2026-06-13.md`, `experiments/exp58_scale_status.py` | BUILT 2026-06-13. Test scale rollup: independently confirmed `147/309` (`48 verified + 99 text-confirmed`), detector scale available `294/309` including tick-only rows, unresolved/fallback `15/309`. Outputs are ignored `results/scale_status_summary.csv` and `results/scale_status_unverified_rows.csv`. | scale is strongly checked but not hidden-label ground truth; avoid broad tail fixes without independent evidence |
| **Segmentation GPU matrix** | `EXP59_SEGMENTATION_GPU_MATRIX_2026-06-13.md`, `experiments/exp59_segmentation_gpu_matrix.py` | BUILT 2026-06-13. Adds configurable GPU training support: image size, architecture, encoder, loss, augmentation, thresholds, batch size, and tagged weights. Ordered next runs: baseline reproduction, high-res 512 U-Net, high-res 512 U-Net++, high-res focal loss, train-time CLAHE. | main next direction; first serious candidate is high-res 512 U-Net after sanity checks |
| **Scale oracle review pack** | `EXP60_SCALE_ORACLE_REVIEW_2026-06-13.md`, `experiments/exp60_scale_oracle_review_pack.py`, `benchmark_lab/scale_review_server.py` | BUILT 2026-06-13. Converts the full 309-image scale partition into a human-oracle review manifest plus a 41-row starter pack: all 15 urgent/fallback rows, 18 tick-only rows spread across the detector distribution, and 8 confident rows for sanity checking. Viewer writes notes/corrections to ignored `results/scale_oracle_review/oracle_notes.json`. | use the user as scale oracle before another scale-only submission; feed confirmed corrections back as explicit rows |
| **Oracle scale patch audit** | `EXP61_ORACLE_SCALE_PATCH_2026-06-13.md`, `experiments/exp61_oracle_scale_patch.py` | BUILT 2026-06-13; UPDATED after full depth review. Depth parsing now normalizes `3.5`, `3.5 cm`, and `35 mm`. Full pass: 193 rows confirm existing scale; 116 are field-depth scale candidates, but many likely reflect field-rectangle overcounting UI height rather than true scale errors. `segment_then_measure.py` has opt-in `UMUD_SCALE_OVERRIDE_CSV` support; default inference does not read human notes. | treat override CSVs as explicit human-reviewed scale probes; do not broad-patch from EXP61 until field-rectangle detection is trusted |
| **Oracle scale submission candidate** | `EXP62_ORACLE_SCALE_SUBMISSION_CANDIDATE_2026-06-13.md`, `experiments/exp62_oracle_scale_submission_candidate.py` | BUILT 2026-06-13. Starts from public-best burn #13/#11 and changes only `IMG_00198-00200` using EXP61 field-depth scale (~159.33 px/cm). Output is ignored `results/submission_burn_18_oracle_scale_198_200_direct.csv`; PA unchanged, FL +10.7 to +16.9 mm on three rows, MT -0.3 to -2.2 mm. | clean isolated scale-only public probe if burning a slot |
| **Depth oracle review UI** | `EXP63_DEPTH_ORACLE_REVIEW_2026-06-13.md`, `experiments/exp60_scale_oracle_review_pack.py`, `benchmark_lab/scale_review_v1/` | BUILT 2026-06-13. Refocuses the scale viewer around proposed field depth, not px/cm. Manifest now has `depth_guess_mm`, `depth_guess_source`, `depth_guess_note`, and `submitted_scale_state`; viewer shortcuts are `Q` correct, `W` wrong, `E` unclear, `A/D` prev/next. | use this for the full 309-row depth confirmation pass before expanding scale overrides |
| **Depth OCR-50 repair** | `EXP63_DEPTH_ORACLE_REVIEW_2026-06-13.md`, `experiments/exp60_scale_oracle_review_pack.py`, `scale_ocr.py` | BUILT 2026-06-13. Full review exposed a single systematic parsing failure: 32 wrong rows all came from stale OCR accepting `50 mm`. The manifest and OCR classifier now repair 1200x800 tick-scale families before trusting raw `50`: 110.7->55mm, 135.4->45mm, 152.3->40mm, 159.5->35mm, 174.0->70mm. Algorithm-only depth audit over all 309 rows now matches the human review 309/309 without using notes as predictor input; sources are 202 OCR text, 50 field-height/scale, 37 tick-family repair, 20 cropped/no-overlay family. Human inputs like `3.5`, `5.5`, and `40 mm` normalize to mm correctly. | depth is now independently guessable; next scale work is separating real px/cm corrections from field-rectangle false candidates |
| **Multi-region text scale OCR audit** | `EXP64_TEXT_SCALE_OCR_AUDIT_2026-06-13.md`, `experiments/exp64_text_scale_ocr_audit.py` | BUILT 2026-06-13. Installs/runs EasyOCR locally in ignored `.venv_ocr`, reads targeted UI crops at 3x, caches tokens, and fuses OCR with non-human fallbacks. Full 309 run: direct OCR depth `237/309`, fused algorithmic depth `309/309`, misses versus human review `0`. Sources: 233 OCR text, 50 numeric left-ruler 50mm labels, 20 cropped/no-overlay family, 6 tick-family repairs. | depth/text side is solved algorithmically; remaining problem is pixel-span-to-depth scale solving, not asking the user to read labels |
| **Conservative 3 cm scale candidates** | `EXP65_SCALE_SOLVER_SUBMISSION_CANDIDATES_2026-06-13.md`, `experiments/exp65_scale_solver_submission_candidates.py` | BUILT 2026-06-13. Creates `results/submission_burn_19_public_best_plus_3cm_scale_198_200_251.csv` from public-best 0.58910, changing only `IMG_00198-00200` plus newly confirmed same-family `IMG_00251` at `478px / 30mm = 159.333 px/cm`. Existing burn #18 changes only `IMG_00198-00200`. #19 adds one row but has MT risk on `IMG_00251` (`-5.434mm`). | submit #18 first; submit #19 only if #18 improves/ties |
| **Robust-triangle repaired-scale candidates** | `EXP66_ROBUST_TRIANGLE_SCALE_CANDIDATES_2026-06-13.md`, `experiments/exp66_robust_triangle_with_scale_candidates.py` | BUILT 2026-06-13. Retests the old robust-triangle public regression under the conservative 3 cm scale repair. Creates burn #20 (`IMG_00198-00200`) and burn #21 (`IMG_00198-00200` plus `IMG_00251`) from burn #15 using robust debug pixels, not approximate public-best deltas. | if burning a benchmark-geometry slot, submit #20; hold #21 unless isolating `IMG_00251` |
| **Field-depth broad scale probe** | `EXP67_FIELD_DEPTH_SCALE_PROBE_2026-06-13.md`, `experiments/exp67_field_depth_scale_probe.py` | BUILT 2026-06-13; PUBLIC-TESTED. Creates burn #22 from public-best burn #13 using EXP64 algorithmic depth plus EXP61 field-rectangle height. Changes 114 rows; PA unchanged; FL mean signed delta `-4.116mm`, MT mean signed delta `-1.211mm`. Public score `0.66197`, a strong regression. | reject as broad default; span heuristic overcorrects, especially 50mm/800px -> 160 px/cm rows |
| **Robust triangle plus broad field-depth scale** | `EXP68_ROBUST_TRIANGLE_FIELD_DEPTH_SCALE_PROBE_2026-06-13.md`, `experiments/exp68_robust_triangle_field_depth_scale_probe.py` | BUILT 2026-06-13. Applies the same EXP67 scale proposals to robust-triangle burn #15, creating burn #23. Changes 114 rows; PA unchanged; FL mean signed delta `-4.301mm`, MT mean signed delta `-1.214mm`. | diagnostic only after #22 regression; not a recommended improvement candidate |
| **Non-full-height field-depth scale probe** | `EXP69_NONFULL_FIELD_DEPTH_SCALE_PROBE_2026-06-13.md`, `experiments/exp69_nonfull_field_depth_scale_probe.py` | BUILT 2026-06-13. Fixes only the worst #22 failure mode by rejecting field spans whose height is basically the whole image (`field_h / image_h >= 0.98`). Creates burn #24 from public-best burn #13 and burn #25 from robust-triangle burn #15. Each changes 9 rows. | superseded by EXP70; #24/#25 are cleaner than #22 but still too loose |
| **Missing-scale-only field-depth probe** | `EXP70_MISSING_SCALE_ONLY_FIELD_DEPTH_PROBE_2026-06-13.md`, `experiments/exp70_missing_scale_only_field_depth_probe.py` | BUILT 2026-06-13. Wires the user's scan-outward field heuristic into EXP61, then applies field-depth scale only where the existing scale partition has no finite px/cm. Creates burn #26 from public-best and burn #27 from robust-triangle, changing only `IMG_00198-00200` plus `IMG_00251` at `478px / 30mm = 159.333 px/cm`. #26 equals older #19 and #27 equals older #21 byte-for-byte. | submit #26 if testing scale now; use #27 only as robust-triangle diagnostic |
| **Local-benchmark proxy plus safe scale** | `EXP71_LOCAL_BENCHMARK_PROXY_SCALE_STACK_2026-06-13.md`, `experiments/exp71_local_benchmark_proxy_scale_stack.py` | BUILT 2026-06-13; PUBLIC-TESTED. Burn #28 scored `0.65917`. It is not burn #15 plus scale: outside the 4 missing-scale rows, its FL equals burn #16's visibility-weighted FL proxy and its MT equals burn #17's vertical-MT proxy. | reject #28; do not promote the local-benchmark proxy stack without production-wiring and target-label validation |
| **Thin-structure segmentation formulation** | `EXP72_THIN_STRUCTURE_SEGMENTATION_2026-06-13.md`, `kaggle_seg72_thin_structure_heavy_auto.ipynb`, `segment_then_measure.py` | BUILT 2026-06-13. Pipeline version `2026-06-13.03` adds soft/dilated fascicle targets, validation threshold sweep, skeleton/skeleton-dilate decoding, heavy augmentation, and prediction-mask debug exports. Partial `seg72_01` log underperformed the EXP59 control (`apo 0.7873` vs `0.7945`; `fasc 0.2594` by epoch 20 vs `0.2925`). | held; do not continue the full EXP72 matrix blindly |
| **Segmentation method audit** | `EXP73_SEGMENTATION_METHOD_AUDIT_2026-06-13.md` | UPDATED 2026-06-13 after a deeper code/notebook/literature audit. Finds that EXP72 is a failed approximation, not a valid test of the full thin-structure literature: it used post-hoc hard skeleton decoding, changed too many knobs at once, lacked target-specific apo/fasc isolation, and lacked probability/decoder/downstream geometry instrumentation. | stop/bundle `seg72_01`; hold remaining EXP72 runs; use EXP74 plan |
| **Controlled segmentation ablation plan** | `EXP74_CONTROLLED_SEGMENTATION_ABLATION_PLAN_2026-06-13.md` | BUILT 2026-06-13. Defines the next notebook: EXP59 control settings, target-specific apo/fasc isolation, decoder sweeps, probability/debug outputs, component counts, accepted fragment counts, geometry distributions, and one-axis target/decoder ablations. | next notebook spec before another long GPU run |
| **Robust-triangle top-boundary candidate** | `SUBMISSION_TRIANGLE_CANDIDATE_2026-06-12.md`, `experiments/exp36_triangle_boundary_candidate.py`, `experiments/exp37_robust_triangle_submission_stack.py` | BUILT 2026-06-12. Production opt-in flag `UMUD_TOP_BOUNDARY_MODE=robust_triangle` changes the upper aponeurosis from one fitted line to the user's robust low-left / high-middle / low-right piecewise triangle. Expert benchmark improves raw true-scale overall `0.251 -> 0.170` and FL `0.519 -> 0.278`; the stacked current-best file is `results/submission_burn_15_temporal_subpixel_shape_ocr_robust_triangle.csv`. | submission-worthy one-slot candidate; record public LB before promoting or rejecting |
| **Shape-neighbor stacked burn pack** | `SUBMISSION_BURN_AFTER_SHAPE_WIN_2026-06-12.md`, `experiments/exp34_after_shape_win_stack.py` | BUILT 2026-06-12 after temporal+subpixel+shape improved public LB to 0.58910. Generates `submission_burn_13_temporal_subpixel_shape_img00275_ocr_scale.csv` and `submission_burn_14_temporal_subpixel_shape_fl_min_extrap_top3.csv`. | submit #13 then #14 if burning the next slots; these replace older #12 and #9 because they keep the #11 gain |
| **Subpixel-stacked burn pack** | `SUBMISSION_BURN_AFTER_SUBPIXEL_WIN_2026-06-12.md`, `experiments/exp33_after_subpixel_win_stack.py` | BUILT 2026-06-12 after temporal+subpixel improved public LB to 0.60936. Generates `submission_burn_11_temporal_subpixel_shape_neighbor_scale.csv` and `submission_burn_12_temporal_subpixel_img00275_ocr_scale.csv`. | submit #11 then #12 if burning the next slots; these replace older unstacked/less-stacked #7 and #8 |
| **Temporal-stacked burn pack** | `SUBMISSION_BURN_AFTER_TEMPORAL_WIN_2026-06-12.md`, `experiments/exp32_temporal_stack_burn_pack.py` | BUILT 2026-06-12 after temporal smoothing improved public LB to 0.60961. Generates follow-ups that keep temporal smoothing as the working baseline: temporal+subpixel, temporal+shape-neighbor, temporal+IMG_00275 OCR scale, temporal+top3 FL, and optional temporal+visibility-weighted FL. | submit the stacked files, not the original unstacked order |
| **Five-slot burn pack** | `SUBMISSION_BURN_2026-06-12.md`, `experiments/exp31_submission_burn_pack.py` | BUILT 2026-06-12. Generates/records five controlled CSV probes from the protected 0.61918 baseline: temporal smoothing, clean shape-neighbor fallback scale, isolated IMG_00275 OCR scale fix, FL top-3 minimal-extrapolation, and FL visibility-weighting. Generated CSVs live in ignored `results/submission_burn_*.csv`; summary lives in ignored `results/submission_burn_pack_2026-06-12_summary.csv`. | if daily slots would expire, submit in the documented order and record public scores back here |
| **Human benchmark lab** | `benchmark_lab/` | BUILT 2026-06-11/12. Includes `make_manifest.py`, `label_server.py`, `score_labels.py`, protocol docs, scratch rulers, draggable review overlays, and trial FL measurement. Generated seed manifests under `results/human_benchmark/`: `public_seed_manifest.csv` (24 public/FALLMUD rows) and `target_seed_manifest.csv` (24 declared human-in-loop target rows, with IMG_00275 forced first). 19/24 target rows were roughly hand-labeled in gitignored local results. Scorer has a light cv2/numpy geometry fallback, so labels can be measured locally without importing the full training stack. | use the Cintiq/review workflow to score the 0.619 baseline and repaired facing/per-gap candidates locally before the next serious submission |
| **Review surfaces** | `benchmark_lab/review_server.py`, `benchmark_lab/generate_synthetic_geometry.py` | BUILT 2026-06-12. Three local viewers exist: target human-in-loop (`--port 8767`), 35-image expert benchmark (`--expert-benchmark --port 8768`), and exact synthetic geometry cases (`--synthetic-dir results/synthetic_geometry --port 8769`). These are decision infrastructure, not submission files. | use synthetic cases as geometry unit tests, the expert viewer as a convention sanity check, and target human rows as the closest local proxy to the hidden leaderboard |
| **OCR scale** (read printed depth/ruler -> px/mm, cross-check vs ticks) | `scale_ocr.py` | DONE 2026-06-10. Full 309-image partition: verified 48, text-confirmed 99, tick-only 147, flag 1, mean 14. 147/148 cross-checks agree; IMG_00275 is the one verified 2x tick-vs-printed-ruler anomaly. | optional one-slot probe: generate an isolated IMG_00275-only OCR scale fix, inspect the one-row diff, and do not combine it with rejected tail/bar/shape changes |

### Benchmark validation of the scale reader (2026-06-10)

IMPORTANT framing: the 35-expert benchmark is scored with the **true** scale from the xlsx, so scale
recovery **cannot move the benchmark FL/MT score** - it is blind to it by construction. What the
benchmark CAN do is validate the *reader* against a known scale. The benchmark images turn out to be
the **same `De XX mm` device family** as the test De-50mm set, but **horizontally mirrored** (`De 65 mm`
reads in reverse; flip with `cv2.flip(im,1)` first). After flipping, the OCR reads a depth on **30/35**,
all sensible (40-65mm), and the read depth is geometrically consistent with the known true scale. So the
reader transfers to ground-truth data. The actual *improvement* from this work lives on the Kaggle test
set (the partition coverage), which the benchmark structurally cannot score.

**Scale-cost experiment** (`experiments/bench_scale_cost.py`): hide the true scale, recover it the way
we must on the test set, score FL/MT with the recovered scale vs the true scale; the gap = what scale
error costs in the real metric. Result: per-image FL/MT (no recenter) with TRUE scale = FL 0.261 / MT
0.179; this **proves scale is load-bearing** - a 2x scale error (what happened on the few benchmark
images our detectors caught) blows FL and MT each up by **+2.4 tolerances**. CAVEAT, the run is
confounded: our tick detectors are tuned for the test families and **failed on the mirrored,
lower-res benchmark** (30/35 fell to PRIOR), so the headline +1.6 overall cost overstates the test-set
cost. The clean signals: (1) scale error is catastrophic, so getting it right is the top lever; (2) the
brittle tick detectors did NOT transfer, but the OCR depth reading DID (30/35) - so OCR depth is the
robust, device-agnostic path and should become the PRIMARY scale source, not the per-family tick rules.
Honest limit: no clean "scale costs X on the test set" number exists (no test ground truth; benchmark
recovery doesn't transfer). Standing position: scale matters hugely; ours is approximately right on test
(no gross errors per MT physiology) but was unverified; the OCR work makes it verifiable + readable.
| **OCR scale reader** (`scale_ocr.py`) | DONE (2026-06-10). Full 309-image partition complete: **verified 48, text-confirmed 99, tick-only 147, flag 1, mean 14**. Where two independent reads exist (148 images), OCR and ticks agree 147/148. The one disagreement is IMG_00275: tick said 201 px/cm, printed ruler says 101 px/cm — caught and quarantined to the prior (not shipped). The 14 `mean` rows and IMG_00275 are genuine unknowns, all others are checkable. | apply OCR scale to IMG_00275 (one-image provably-correct fix) |
| **Per-gap multi-level** (assign fascicles to gaps, geometry per gap) | `experiments/per_gap_viewer.py` | REWORKED 2026-06-10 to the user's diagnosis: (1) `apo_bands` MERGES mask fragments that overlap in depth into one aponeurosis and keeps depth-separated ones distinct; (2) hard guards — reject a gap whose two apo lines cross, drop any fascicle whose superficial end isn't strictly above its deep end; (3) draw the real mask edge per-x. Prototype on ~16 images via `UMUD_PERGAP_PROTO=1`. NOT wired to production, NOT submitted. | wire facing-FL per gap (use per-gap for multi-muscle separation ONLY; FL must use `compute_facing_fl()` / minimize-extrapolation, NOT the per-gap wave trace which overshoots +24mm) |
| **FL recenter** (`segment_then_measure.py:762`) | PROVEN NO-OP on the shipped pipeline. Running `UMUD_FL_RECENTER=0` changes 0/309 rows — per-image FL already averages 74.4 before the pin step. The recenter fires only when the mean deviates from PRIOR=74.424, and it doesn't. Not a hidden confounder on the current pipeline. | no action needed unless FL method changes |

## Wanted but unbuilt / uncertain

- **Facing-FL per gap**: wire the per-gap multi-muscle separation from `per_gap_viewer.py`'s `apo_bands()` + gap formation, but compute FL using `compute_facing_fl()` (zero-bias), NOT the wave trace (which overshoots +25mm). This is the highest-value next submission target — it fixes the ~13 multi-muscle images that caused facing's LB regression without losing facing's zero-bias gain on normal images.
- **IMG_00275 scale fix**: apply OCR scale (101 px/cm) instead of the wrong tick scale (201 px/cm). One image, provably correct.
- **Fascicle segmentation**: the real bottleneck. The FL gap to DL-Track is mask quality, not geometry. Recall-bias retrain already failed. Needs better training data or architecture, not more post-processing.
- **Scale classifier** (route by device family, then OCR the exact depth). Family is learnable; exact depth must still be read (3.5 vs 4.5cm look identical otherwise).

## New diagnostic findings (2026-06-10/12)

| finding | tool | result |
|---------|------|--------|
| **Immediate submission audit** | CSV diff against `results/submission_local.csv` | No new ready 4-slot burn exists. `submission_subpixel_scale.csv` is tiny (FL mean abs delta 0.094mm, max 0.673mm); `submission_host_mt_vertical3_no_subpixel.csv`, scale-tail/bar-only, facing-FL, and identity blend are known rejected or audited worse than 0.61918. Only a clean IMG_00275-only OCR scale fix is defensible as a quick one-row probe. |
| **Target human labels and review tooling** | `benchmark_lab/label_server.py`, `benchmark_lab/review_server.py`, `benchmark_lab/score_labels.py` | 19/24 target rows have rough local hand labels in ignored `results/human_benchmark/`. The viewer can compare human masks, baseline, candidates, scale, rulers, PA/FL/MT, and scratch trial FLs. Treat these labels as triage, not official truth, but use them before another broad submission. |
| **Exact synthetic geometry benchmark** | `benchmark_lab/generate_synthetic_geometry.py`, `benchmark_lab/SYNTHETIC_ABSTRACT_BRIEF.md` | Built abstract boundary/strand cases with known exact geometry. First generated pack is useful for testing straight, steep, curved, and fan-like geometry assumptions. It is not realistic ultrasound and should not be used as leaderboard evidence by itself. |
| **Expert benchmark review mode** | `benchmark_lab/review_server.py --expert-benchmark` | Built a visual review mode for the 35-image expert set comparing robust expert consensus, our true-scale pipeline, DLTrack, SMA, scratch measurements, and diagnostic line overlays. On 2026-06-12 the local benchmark truth was made robust to clear single-rater tails without editing the source xlsx: `im_19_arch` MT drops `R7=80.13` (mean `30.03 -> 20.01`) and `im_26_arch` FL drops `R7=33.88` (mean `64.11 -> 70.16`). Full harness is now `0.1909` (PA `0.1498`, FL `0.3390`, MT `0.0840`); raw true-scale CSV is `0.251` (PA `0.150`, FL `0.519`, MT `0.084`). |
| **Benchmark error taxonomy** | `experiments/exp35_benchmark_error_taxonomy.py`, `benchmark_error_taxonomy.md` | BUILT 2026-06-12. Classifies each 35-expert image by geometry failure mode and writes local `results/benchmark_error_taxonomy.{csv,md}`. Naive wrong-way pruning does not help locally (`0.251 -> 0.252` for both signed-angle and raw-slope variants). The stronger tail/aggregation signal is projected-FL percentile: p25 improves the local raw true-scale score to `0.172` (FL `0.281`) because many expert values sit below our projected median. The user's actual triangle boundary idea, tested after the first straight-line misread, is strongest locally: robust triangle `0.170` (FL `0.278`), exact triangle `0.182` (FL `0.314`); on `im_29_arch` exact triangle gives median FL `75.81mm` vs expert `75.30mm`. Viewer now shows taxonomy tags, projected cyan fragment spans, and per-image projection-tail diagnostics. | robust triangle has been promoted to one controlled candidate via `SUBMISSION_TRIANGLE_CANDIDATE_2026-06-12.md`; still do not make it the default unless public LB agrees |
| **FL methods side-by-side** | `bench_fl_methods.py` | straight all-frags: bias +24mm, term 2.02; **facing: bias +0.7mm, term 0.26 (zero bias)**; per-gap wave: bias +25mm, term 2.1 (same as straight - it dropped minimize-extrapolation). The overshoot is the disease; minimize-extrapolation is the cure. |
| **Fascicle bend effect** | `bench_fl_bend.py` | bend is anatomically real (FALLMUD: parabola fits apo edges 44% better than line). But the competition FL ground truth is the raters' straight-line extrapolation convention, not the true curved anatomy. Bend diverges from the scored convention and is below tolerance (marginal and not worth chasing). |
| **FALLMUD transfer** | `fallmud_fl_test.py` | Our wave FL is median 1.09x longer than the reference straight-extrapolation FL on independent NeilCronin GT masks - confirms wave overshoots, facing's minimize-extrapolation is what keeps it honest. |
| **PA geometry validated** | `compare_lines.py` + `draw_tool.py` | User hand-drew fascicles on 8 test images. Our fitted angle field is off by mean 1.8°, median 1.4°, 90th pct 3.8° — well inside the 6° PA tolerance. PA geometry is confirmed good. |
