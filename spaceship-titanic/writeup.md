# Spaceship Titanic

## Question

Predict `Transported`, a boolean recording whether a passenger was transported to an alternate dimension. One row is one passenger, with demographic and onboard-spending attributes. The metric is classification accuracy, the fraction of passengers predicted correctly.

## Method

Pending. Planned: a gradient-boosting baseline (LightGBM) with StratifiedKFold cross-validation, with the split changed to GroupKFold on the travel group if group membership proves to leak. Features and the final model set will be recorded here as they are fixed.

## Result

Pending. Local cross-validation and public leaderboard scores will be reported in the table below, numbers first.

| Model | Features | Local CV (accuracy) | Public LB (accuracy) |
| --- | --- | --- | --- |
| (pending) | | | |

## Caveat

Pending. This section will state what the result does not establish, for example the gap between the public and private leaderboards and the limits of the validation scheme.

## Lesson

Pending. This section will record what moved the score, what failed, and what to try next.
